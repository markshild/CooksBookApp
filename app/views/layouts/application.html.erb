<!DOCTYPE html>
<html>
<head>
  <title>CooksBookApp</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<header class="group">
  <a href="#" class="logo">CooksBook</a>
  <% if signed_in? %>
  <nav class="user-nav">
    <h6 class="name"><%= current_user.name %><div class="dropdown">
      <a href="#/users/current">My Page</a>
      <%= button_to "Sign Out", session_url, :method => :delete %></h6>

    </div>
    <a class="add "href="#/recipes/new">+</a>
  </nav>
  <% else %>
  <ul>
    <li><%= link_to "Sign In", new_session_url %></li>
    <li><%= link_to "Sign Up", new_user_url %></li>

  </ul>
  <% end %>
</header>
<body>


<%= yield %>

</body>
</html>
